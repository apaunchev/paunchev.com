{
  "name": "paunchev.space",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "yarn run build && next export",
    "deploy": "yarn export && touch out/.nojekyll && gh-pages -t -d out"
  },
  "dependencies": {
    "next": "^9.4.0",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  }
}
