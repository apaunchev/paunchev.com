---
title: 'useStickyState'
updatedAt: '2022-01-03 12:00:00'
---

Quick and easy way to add some basic persistence to your app.

```jsx
function useStickyState(defaultValue, key) {
  const [value, setValue] = React.useState(() => {
    const stickyValue = window.localStorage.getItem(key);

    return stickyValue !== null ? JSON.parse(stickyValue) : defaultValue;
  });

  React.useEffect(() => {
    window.localStorage.setItem(key, JSON.stringify(value));
  }, [key, value]);

  return [value, setValue];
}
```

Usage:

```jsx
const SomeComponent() {
  const [color, setColor] = useStickyState('blue', 'persisted-color');
}
```

---

Source: https://www.joshwcomeau.com/snippets/react-hooks/use-sticky-state/
