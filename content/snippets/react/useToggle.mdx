---
title: 'useToggle'
updatedAt: '2022-01-04 17:00:00'
---

A wrapper for `useState` specifically made for boolean values that need to be toggled on some action.

```jsx
export default function useToggle(initialValue = false) {
  const [value, setValue] = React.useState(initialValue);
  const toggle = React.useCallback(() => {
    setValue(v => !v);
  }, []);

  return [value, toggle];
}
```

Usage:

```jsx
const App = () => {
  const [isOn, toggleIsOn] = useToggle();

  return (
    <>
      {isOn ? 'The light is on!' : 'Hey who turned off the lights'}
      <button onClick={toggleIsOn}>Press me</button>
    </>
  );
};
```

---

Source: https://www.joshwcomeau.com/snippets/react-hooks/use-toggle/
