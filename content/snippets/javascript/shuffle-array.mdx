---
title: 'Shuffle array'
updatedAt: '2022-01-02 12:00:00'
---

```javascript
const shuffle = array =>
  array
    .map(a => ({ sort: Math.random(), value: a }))
    .sort((a, b) => a.sort - b.sort)
    .map(a => a.value);
```
