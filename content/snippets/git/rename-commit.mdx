---
title: Rename a commit
---

To rename the most recent commit:

`git commit --amend -m "The correct message"`

If the commit is older, then do an interactive rebase:

`git rebase -i [hash]`

In the editor, change `pick` to `reword` and save.
